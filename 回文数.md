/**
 * @param {number} x
 * @return {boolean}
 */
var isPalindrome = function (x) {
  if (typeof x !== 'number') return false;

  const s = x + '';
  if (s === s.split('').reverse().join()) return true;
  return false;
};
